var job = new CronJob(
        `${notes}`,
        function() {
          console.log('You will see this message every second');
        },
        null,
        true,
        'America/Los_Angeles'
      );

      const upcoming = new Upcoming({ title, description, reminder });
      await upcoming.save();
      return upcoming;